import React from "react";
import styled from "styled-components";

const None = () => {
  return <Wrapper>검색 결과 더 보기</Wrapper>;
};

const Wrapper = styled.div`
  width: 100%;
`;

export default None;
